import GraphNode from "./GraphNode";

export default class Graph {
  private flag: boolean; 
  
  //constructor
  constructor(){
    this.flag = true; 
  }
  add(node : GraphNode){return;}
  size(){return;} //TODO add array of graph nodes
  addAll(node : GraphNode[]){return;}
}
